<Window x:Class="HelloWPF.GridColumns"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWPF"
        mc:Ignorable="d"
        Title="GridColumns" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Button1" VerticalAlignment="Top" HorizontalAlignment="Center" Height="34" Margin="176,0,160.8,0" Width="60" />
        <Button Content="Button2"  Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Height="35" Margin="0,185,-0.4,200" Width="57"/>

    </Grid>
</Window>
